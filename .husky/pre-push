#!/bin/sh
# Always run from the repo root
REPO_ROOT=$(git rev-parse --show-toplevel)
cd "$REPO_ROOT" || exit 1

# Get the current branch name
BRANCH=$(git rev-parse --abbrev-ref HEAD)

# Fetch the latest remote info
git fetch origin "$BRANCH"

# Check for committed changes in the frontend directory that will be pushed
changed_files=$(git diff --name-only origin/$BRANCH)

should_check_frontend=false
for file in $changed_files; do
  case $file in
    frontend/*)
      should_check_frontend=true
      break
      ;;
  esac
done

if [ "$should_check_frontend" = true ]; then
  echo "\n🔎 Running lint and build checks for frontend..."
  cd frontend || exit 1
  pnpm lint
  if [ $? -ne 0 ]; then
    echo "\n❌ Lint errors found in frontend. Please fix them before pushing."
    exit 1
  fi
  pnpm build
  if [ $? -ne 0 ]; then
    echo "\n❌ Build failed in frontend. Please fix build errors before pushing."
    exit 1
  fi
  cd "$REPO_ROOT" || exit 1
  echo "\n✅ Pre-push checks passed. Proceeding with push."
  exit 0
fi

echo "\n✅ No frontend changes detected. Proceeding with push."
